# Production build env (CRA automatski učita pri `yarn build`).
# Frontend zove backend preko istog domena (/api/), pa može i prazno biti;
# eksplicitno setujemo radi konzistentnosti i ako se direktno koristi axios baseURL.
REACT_APP_BACKEND_URL=https://laptopia.rs
